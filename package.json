{
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": " cd frontend && npm run preview",
    "deploy": "cd frontend && npm run build && cd ../backend && node server.js",
    "start": "cd backend && node server.js"
  }
}
